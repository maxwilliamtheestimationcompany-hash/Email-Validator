{
  "name": "email-validator-dedicated-backend",
  "version": "6.4.0",
  "private": true,
  "description": "Validect RapidAPI email validator with saved bulk jobs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js",
    "test": "node --test validect.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "latest",
    "dotenv": "latest",
    "express": "latest",
    "validator": "latest"
  }
}
